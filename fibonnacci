function getFib(n) {
    function go(i, preval, val) {
        if (i >= n) {
            return val;
        } else {
            return go(i + 1, val, preval + val);
        }
    }
    if (n === 0) {
        return 0;
    } else {
        return go(1, 0, 1);
    }
}




